name: Primeiro Workflow

on:
  push:

jobs: 
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Mostrar data
        run: date

      - name: Mostrar diretório
        run: pwd

      - name: Listar arquivos
        run: ls -la
